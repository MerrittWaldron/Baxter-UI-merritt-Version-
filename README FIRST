Baxter GUI environment setup procedure

#from conda base env

#this will create the correct environemnt to build the BaxterGUI code
#open an Anaconda prompt window
#cd to the BaxterGUI directory
#execute these commands

conda env create --name baxterGUI --file=package.yml
conda activate BaxterGUI

#these command are optional if the shareplum or pyinstaller packages were not installed
pip3 install shareplum 0.5.1
pip3 install pyinstaller==5.6.2

